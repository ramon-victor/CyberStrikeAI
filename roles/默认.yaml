name: Default
name_en: Default
description: Default role with no additional user prompt; uses the default MCP configuration
description_en: Default role with no additional user prompt; uses the default MCP configuration
user_prompt: ""
icon: "\U0001F535"
enabled: true
