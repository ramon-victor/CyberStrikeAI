# Plan: Merge Upstream v1.6.36 + Fix Translations + Docker Bug

## Context

- **Fork**: `ramon-victor/CyberStrikeAI` (your repo)
- **Upstream**: `Ed1s0nZ/CyberStrikeAI` (original repo)
- **Last sync**: v1.6.29 (commit `e5533e7`)
- **Current upstream**: v1.6.36 — ~40+ new commits to merge
- **Your customizations**: Docker files, GitHub workflows, English translations, port 8080→7022, brand changes

## Step 1: Fix Docker Build Bug

**File**: `Dockerfile:18`

The `tools-builder` stage uses `golang:1.26-bookworm` which doesn't exist (Go 1.26 not released). Should be `golang:1.24-bookworm` to match the `app-builder` stage on line 3.

```dockerfile
# Line 18: Change FROM golang:1.26-bookworm AS tools-builder
# To:       FROM golang:1.24-bookworm AS tools-builder
```

## Step 2: Merge Upstream Changes (Full Git Merge)

### Strategy: Full git merge with manual conflict resolution

```bash
git merge upstream/main
```

**User decision**: Full git merge (not selective). Update version to v1.6.36.

### Expected conflict zones (your files that upstream also changed):

| File | Your changes | Upstream changes | Resolution |
|------|-------------|-----------------|------------|
| `config.yaml` | Port 7022, English comments, v1.6.29 | New sections (vision, robots), v1.6.36 | Keep your port/translations, adopt new sections |
| `web/templates/index.html` | lang="en-US", translated UI | New features (robot commands, vuln search) | Keep lang="en-US", merge new features |
| `web/static/js/settings.js` | English translations | Feature changes | Keep English, merge features |
| `web/static/js/dashboard.js` | English translations | Feature changes | Keep English, merge features |
| `web/static/js/chat.js` | English translations | Feature changes | Keep English, merge features |
| `internal/app/app.go` | Port/config changes | Feature additions | Careful merge |
| `agents/*.md` | English translations | Content updates | Keep English, merge new content |

### Files ONLY in your fork (no conflict expected):
- `Dockerfile`, `docker-compose.yml`, `docker-compose.gluetun.yml`
- `.env`, `.dockerignore`, `.gitattributes`
- `.github/workflows/docker-image.yml`
- `config.docker.yaml`
- `scripts/docker/` (if exists)

## Step 3: Translate Remaining Chinese to English

### High priority (user-facing):
1. `web/templates/api-docs.html` — 31+ Chinese strings (page title, auth instructions, language selector)
2. `web/static/js/settings.js` — Chinese comments and fallback strings
3. `web/static/js/dashboard.js` — Chinese fallback strings
4. `web/static/js/chat.js` — Chinese fallback strings
5. All other `web/static/js/*.js` files — Chinese comments and `window.t()` fallback strings

### Approach:
- For `data-i18n` attributes: upstream uses i18n keys, so these are already covered by en-US.json
- For hardcoded Chinese fallback strings: translate to English
- For Chinese comments: translate to English

## Step 4: Verify Translations

After merge and translation fixes:
1. Check en-US.json is complete (no missing keys)
2. Grep for remaining Chinese characters in web/ directory
3. Verify `lang="en-US"` is set in index.html
4. Verify DEFAULT_LANG = 'en-US' in i18n.js

## Step 5: Test Docker Build

```bash
docker compose -f docker-compose.gluetun.yml up -d --build
```

If `.env` has valid WireGuard credentials, this should build and start both services.

## Files to Modify

1. `Dockerfile` — Fix golang version
2. `config.yaml` — Merge upstream + preserve customizations
3. `web/templates/index.html` — Merge upstream + preserve English
4. `web/templates/api-docs.html` — Translate Chinese to English
5. `web/static/js/*.js` — Translate remaining Chinese fallback strings
6. `agents/*.md` — Merge upstream + preserve English translations

## Verification

1. `git diff --stat` to review all changes
2. `grep -r '[\x{4e00}-\x{9fff}]' web/` to find remaining Chinese
3. `docker compose -f docker-compose.gluetun.yml up -d --build` to test Docker
4. `go build ./cmd/server/main.go` to verify Go compilation
