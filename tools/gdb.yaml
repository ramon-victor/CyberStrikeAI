name: "gdb"
command: "gdb"
enabled: true
short_description: "GNU Debugger for binary analysis and debugging"
description: |
  GDB is the GNU Debugger, used for debugging and analyzing binary programs.

  **Key Features:**
  - Program debugging
  - Memory analysis
  - Disassembly
  - Breakpoint setting

  **Use Cases:**
  - Binary analysis
  - Vulnerability research
  - Reverse engineering
  - Program debugging
parameters:
  - name: "binary"
    type: "string"
    description: "Path to the binary file to analyze"
    required: true
    position: 0
    format: "positional"
  - name: "commands"
    type: "string"
    description: "GDB commands to execute (semicolon-separated) "
    required: false
    flag: "-ex"
    format: "flag"
  - name: "script_file"
    type: "string"
    description: "GDB script file path"
    required: false
    flag: "-x"
    format: "flag"
  - name: "additional_args"
    type: "string"
    description: |
      Additional gdb parameters. Used to pass gdb options not defined in the parameter list.

      **Example Values:**
      - Add common parameter examples based on the tool

      **Notes:**
      - Separate multiple parameters with spaces
      - Ensure correct parameter format to avoid command injection
      - This parameter is appended directly to the end of the command
    required: false
    format: "positional"
