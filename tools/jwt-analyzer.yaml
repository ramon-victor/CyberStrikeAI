name: "jwt-analyzer"
command: "jwt_tool"
enabled: true
short_description: "JWT token analysis and security testing tool"
description: |
  Advanced JWT token analysis and vulnerability testing tool, used for detecting security issues in JWT implementations.

  **Key Features:**
  - JWT token analysis
  - Vulnerability testing
  - Attack vector detection
  - Token manipulation

  **Use Cases:**
  - JWT security testing
  - Token analysis
  - Vulnerability discovery
  - Security testing
parameters:
  - name: "jwt_token"
    type: "string"
    description: "JWT token to analyze"
    required: true
    flag: "-t"
    format: "flag"
  - name: "target_url"
    type: "string"
    description: "Optional target URL for testing token manipulation"
    required: false
    flag: "-u"
    format: "flag"
  - name: "additional_args"
    type: "string"
    description: |
      Additional jwt-analyzer arguments. Used to pass jwt-analyzer options not defined in the parameter list.

      **Example Values:**
      - Add common parameter examples based on the tool

      **Notes:**
      - Separate multiple parameters with spaces
      - Ensure correct parameter format to avoid command injection
      - This parameter is appended directly to the end of the command
    required: false
    format: "positional"
