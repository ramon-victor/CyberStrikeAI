name: "xsser"
command: "xsser"
enabled: true
short_description: "XSS vulnerability scanner and exploitation tool"
description: |
  XSSer is an automated XSS vulnerability testing tool.

  **Key Features:**
  - XSS vulnerability detection
  - Multiple XSS techniques
  - Automated testing
  - Report generation

  **Use Cases:**
  - XSS vulnerability testing
  - Web application security testing
  - Penetration testing
  - Vulnerability verification
parameters:
  - name: "url"
    type: "string"
    description: "Target URL"
    required: true
    flag: "--url"
    format: "flag"
  - name: "params"
    type: "string"
    description: "Parameters to test"
    required: false
    flag: "--Fp"
    format: "flag"
  - name: "additional_args"
    type: "string"
    description: |
      Additional xsser arguments. Used to pass xsser options not defined in the parameter list.

      **Example Values:**
      - Add common parameter examples based on the tool

      **Notes:**
      - Separate multiple parameters with spaces
      - Ensure correct parameter format to avoid command injection
      - This parameter is appended directly to the end of the command
    required: false
    format: "positional"
